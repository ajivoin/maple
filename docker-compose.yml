services:
  maple:
    build: .
    image: maple:latest
    container_name: maple
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATABASE_PATH: /data/maple.db
    volumes:
      - maple_data:/data

volumes:
  maple_data:
