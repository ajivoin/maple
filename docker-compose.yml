services:
  maple:
    build: .
    image: maple:latest
    container_name: maple
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      NODE_ENV: production
